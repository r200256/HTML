(function(){
    // глобальная переменная нашего скрипта
var message = "Hello ";

// функция для вывода этой переменной
function showMessage() {
  console.log(message);
}

// выводим сообщение
showMessage();
}());

